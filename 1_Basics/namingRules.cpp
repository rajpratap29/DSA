#include<iostream>
using namespace std;
int main(){
    // int int = 1; it will give error because there are some name which are reserved keywords
    // a b c d A B C D, _ , $ name can be start with these but most standard way is is abc or _
    // special character like ! @ # etc... can not be used 
    // try to give meaningfull name like area rather than a
    return 0;
}