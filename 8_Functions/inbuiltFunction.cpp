#include<iostream>
#include<cmath>
using namespace std;
int main(){
    int x,y;
    cout<<"Enter two values to check minimum: ";
    cin>>x>>y;
    cout<<min(x,y)<<endl;
    int root;
    cout<<"Enter values to check square root: ";
    cin>>root;
    cout<<sqrt(root)<<endl;
    int a,b;
    cout<<"Enter two values to check maximum: ";
    cin>>x>>y;
    cout<<max(x,y)<<endl;

    return 0;
}