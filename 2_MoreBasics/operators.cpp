#include<iostream>
using namespace std;
int main(){
    int x = 3;
    cout<<(x==3);

    // more relational operator ==, !=, >=, <=, >, <

    // assignment operator =, +=, -=, /=, *=, %=

    return 0;
}